<nav class="navbar navbar-expand-lg navbar-light  navbar-fixed-top nav1">
    <div class="container">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item @(ViewContext.RouteData.Values["Action"].ToString() == "About" ? "active" : "")">
                    @Html.ActionLink("O projektu", "About", "Home", new { }, new { @Class = "nav-link" })
                </li>
                @if (!User.Identity.IsAuthenticated)
                {
                    <li class="nav-item @(ViewContext.RouteData.Values["Action"].ToString() == "Login" ? "active" : "")">
                        @Html.ActionLink("Prijava", "Login", "Home", new { }, new { @Class = "nav-link" })
                    </li>
                    <li class="nav-item @(ViewContext.RouteData.Values["Action"].ToString() == "Register" ? "active" : "")">
                        @Html.ActionLink("Registracija", "Register", "Home", new { }, new { @Class = "nav-link" })
                    </li>
                }
                else
                {
                    <li class="nav-item">
                        <a class="nav-link" href="#">Farme </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Oranice</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Strojevi</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link disabled" href="#">Mljekomati</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link disabled" href="#">Ostalo</a>
                    </li>
                }
            </ul>
            @if (User.Identity.IsAuthenticated)
            {
            <ul class="navbar-nav ml-auto">
                <li class="nav-item @(ViewContext.RouteData.Values["Action"].ToString() == "Index" ? "active" : "")">
                    @Html.ActionLink("Profil", "Index", "Home", new { }, new { @Class = "nav-link" })
                </li>
            </ul>
            }
            </div>
    </div>
</nav>